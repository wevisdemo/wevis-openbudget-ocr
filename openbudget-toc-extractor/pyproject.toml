[project]
name = "toc_extractor"
version = "0.0.1"
dependencies = [
    "PyMuPDF"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv]
package = true